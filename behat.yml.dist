default:
    extensions:
        Behat\MinkExtension:
            # Change this to your web server address.
            base_url: http://localhost:8888
            sessions:
                default:
                    goutte: ~
            show_auto: false
            show_cmd: chromium-browser %s
